# Agent API

FastAPI service for the ecommerce after-sales Copilot.

Current endpoints:

- `GET /health`
- `POST /api/chat`

Run locally after installing dependencies:

```bash
uvicorn app.main:app --reload --port 8000
```
